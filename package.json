{
  "name": "vue",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "serve": "cd MAOL/vue && npm i && npm run serve",
    "build": "cd MAOL/vue && npm i && npm run build",
    "lint": "cd MAOL/vue && npm i && npm run lint"
  },
  "engines": {
    "node": "14.x"
  }
}
