{
  "name": "vue",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "serve": "cd MAOL/vue && npm i && vue-cli-service serve",
    "build": "cd MAOL/vue && npm i && vue-cli-service build",
    "lint": "cd MAOL/vue && npm i && vue-cli-service lint"
  }
}
